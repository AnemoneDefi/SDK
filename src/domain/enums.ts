export enum SwapDirection {
  PayFixed = 0,
  ReceiveFixed = 1,
}

export enum LpStatus {
  Active = 0,
  Withdrawn = 1,
}

export enum PositionStatus {
  Open = 0,
  Matured = 1,
  Liquidated = 2,
  ClosedEarly = 3,
}

export enum MarketStatus {
  Active = 0,
  Paused = 1,
}
